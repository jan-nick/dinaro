import { AutoAnimateDirective } from './auto-animate.directive';

describe('AutoAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
